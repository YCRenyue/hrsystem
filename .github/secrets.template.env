# GitHub Secrets 配置模板
# 复制此文件并填写你的值，然后添加到GitHub Secrets
# 不要将填写后的版本提交到git！

# ========================================
# 阿里云访问配置
# ========================================
ALIYUN_ACCESS_KEY_ID=你的AccessKey-ID
ALIYUN_ACCESS_KEY_SECRET=你的AccessKey-Secret
ALIYUN_REGION=cn-beijing

# ========================================
# 阿里云容器镜像服务 (ACR)
# ========================================
ACR_REGISTRY=registry.cn-beijing.aliyuncs.com
ACR_NAMESPACE=hrsystem
ACR_USERNAME=你的ACR用户名
ACR_PASSWORD=你的ACR密码

# ========================================
# ECS实例配置
# ========================================
# 后端ECS内网IP（逗号分隔）
BACKEND_ECS_IPS=172.16.0.10,172.16.0.11

# 前端ECS内网IP（逗号分隔）
FRONTEND_ECS_IPS=172.16.1.10,172.16.1.11

# ECS访问的SSH私钥
# 生成方式: ssh-keygen -t rsa -b 4096 -f ~/.ssh/aliyun_ecs
# 然后复制私钥内容到这里
ECS_SSH_KEY=-----BEGIN OPENSSH PRIVATE KEY-----
你的SSH私钥内容
-----END OPENSSH PRIVATE KEY-----

# ========================================
# 负载均衡器 (SLB) 配置
# ========================================
BACKEND_SLB_ID=lb-xxxxxxxxxxxxx
FRONTEND_SLB_ID=lb-xxxxxxxxxxxxx

# ========================================
# 应用配置
# ========================================
# 前端将使用的后端API地址
BACKEND_API_URL=https://api.yourdomain.com

# ========================================
# 数据库配置
# ========================================
DB_HOST=rm-xxxxxxxxxxxxx.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_NAME=hrsystem
DB_USER=hrsystem_user
DB_PASSWORD=你的安全数据库密码

# ========================================
# Redis配置
# ========================================
REDIS_HOST=r-xxxxxxxxxxxxx.redis.rds.aliyuncs.com
REDIS_PORT=6379

# ========================================
# 安全配置
# ========================================
# JWT密钥（至少32字符）
# 生成方式: openssl rand -base64 32
JWT_SECRET=你的JWT密钥至少32字符

# AES-256加密密钥（正好32字符）
# 生成方式: openssl rand -base64 32 | head -c 32
ENCRYPTION_KEY=你的加密密钥正好32个字符长度

# ========================================
# 使用方法:
# ========================================
# 1. 复制此文件: cp secrets.template.env secrets.env
# 2. 在secrets.env中填写所有值
# 3. 将每个secret添加到GitHub:
#    - 进入 Settings → Secrets and variables → Actions
#    - 点击 "New repository secret"
#    - 添加上述每个secret的名称和值
# 4. 添加完成后删除secrets.env
# 5. 绝对不要将secrets.env提交到git！
